# Expense-Manager
A fully client-side expense management web app using vanilla JavaScript and localStorage. Includes transaction CRUD, custom categories, monthly budgets with smart alerts, calendar visualization, Chart.js analytics, and a polished settings UI.
